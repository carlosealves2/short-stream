{
  "name": "video-feed-provider",
  "sourceRoot": "apps/video_feed_provider/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/video_feed_provider"],
      "options": {
        "command": "pnpm build",
        "cwd": "apps/video_feed_provider"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm start:dev",
        "cwd": "apps/video_feed_provider"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm test",
        "cwd": "apps/video_feed_provider"
      }
    },
    "test:cov": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm test:cov",
        "cwd": "apps/video_feed_provider"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm lint",
        "cwd": "apps/video_feed_provider"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm format",
        "cwd": "apps/video_feed_provider"
      }
    }
  },
  "tags": ["type:app", "lang:node", "scope:video", "framework:nestjs"]
}
