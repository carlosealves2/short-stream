apiVersion: v1
kind: Service
metadata:
  name: video-schene-analyzer
  namespace: short-stream
spec:
  selector:
    app: video-schene-analyzer
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
