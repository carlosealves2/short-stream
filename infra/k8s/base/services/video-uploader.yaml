apiVersion: v1
kind: Service
metadata:
  name: video-uploader
  namespace: short-stream
spec:
  selector:
    app: video-uploader
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
