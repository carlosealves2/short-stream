apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: short-stream
spec:
  selector:
    app: notification-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
